---
"@eigeninteractive/kernel": patch
"@eigeninteractive/rules": patch
"@eigeninteractive/server": patch
"@eigeninteractive/testkit": patch
"create-eigen-game": patch
---

Use **EigenInteractive** as the product name throughout, matching the domain,
the npm scope and the GitHub organization. Package descriptions, READMEs and
the OpenAPI document title change; every identifier — `@eigeninteractive/*`,
`eigen_flutter`, `create-eigen-game`, the `Eigen-Signature` header — is
untouched.
