---
"create-eigen-game": patch
---

Say in the generated README that `firebase:configure` asks which Firebase
project to use, and how to name it up front with
`run firebase:configure -- --project my-project-id`.
