{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // This package is Node-side tooling -- the contract emitter, the inspect
    // CLI, local state -- so name the Node ambient types it actually uses.
    "types": ["node"]
  },
  "include": ["src"]
}
