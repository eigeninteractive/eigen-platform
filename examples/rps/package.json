{
  "name": "rps",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "test": "vitest run && vitest run -c vitest.unit.config.mts",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json"
  },
  "devDependencies": {
    "@eigen/testkit": "workspace:*",
    "@types/node": "^26.1.1"
  },
  "dependencies": {
    "@eigen/rules": "workspace:*",
    "zod": "^4.4.3"
  }
}
